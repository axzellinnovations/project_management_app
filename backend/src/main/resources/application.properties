spring.application.name=backend

#spring.datasource.url= ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/planora_db}
#spring.datasource.username= ${SPRING_DATASOURCE_USERNAME:titan}
#spring.datasource.password= ${SPRING_DATASOURCE_PASSWORD:2024}

spring.datasource.url= ${SPRING_DATASOURCE_URL:jdbc:postgresql://db.htwixgmokxaunwwogrfo.supabase.co:5432/postgres}
spring.datasource.username= ${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password= ${SPRING_DATASOURCE_PASSWORD:Planora2026@}
spring.datasource.driver-class-name=org.postgresql.Driver

#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.defer-datasource-initialization=false

jwt.secret=${JWT_SECRET}

spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# application.properties
aws.accessKeyId=${AWS_ACCESS_KEY}
aws.secretKey=${AWS_SECRET_KEY}
aws.region=eu-north-1
aws.s3.profile-bucket=planaro-profile-photos
aws.s3.dms-bucket=planaro-dms-storage